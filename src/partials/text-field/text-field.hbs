<div class="text-field">
    <input class="text-field__input" type="{{type}}" name="{{ name }}" placeholder="{{placeholder}}">
    <label class="text-field__label" for="{{ name }}">{{ label }}</label>
</div>
